بناءً على الكود الذي أرسلته لملف **`TableDefinitionEntity.cs`**، قمت باستخراج "دليل التعبئة" (Cheat Sheet) الدقيق.

هذا الكود يعتمد على **فصل المسؤوليات** (حقائب `UX_CONFIG` و `SYS_CONFIG`) واستخدام **`DataSanitizer`** لتنظيف المفاتيح تلقائياً.

إليك ما يجب أن يكون داخل Google Sheet بالضبط:

---

# 📋 دليل تعبئة جدول: SYS_DEFINITIONS

**اسم الشيت (Sheet Name):** `SYS_DEFINITIONS`

### 1. الأعمدة الصريحة (Headers)
هذه هي العناوين التي يجب أن تضعها في الصف الأول من ملف Google Sheet. (الترتيب لا يهم، لكن الأسماء يجب أن تتطابق حرفياً).

| اسم العمود (Header) | نوع البيانات | وصف وملاحظات (بناءً على الكود) |
| :--- | :--- | :--- |
| **`ENTITY_KEY`** | Text | **المفتاح الأساسي**. يجب أن يكون إنجليزي (A-Z, 0-9, _). الكود سيقوم بتنظيفه تلقائياً. |
| **`DISPLAY_NAME`** | Text | الاسم الظاهر للمستخدم (عربي/إنجليزي). |
| **`ENTITY_TYPE`** | Text | نوع الجدول (يستخدم `SystemLookups`). القيم: `COST`, `PERF`, `REF`, `COMP`. |
| **`BUSINESS_DOMAIN`** | Text | المجال. القيم: `MATERIAL`, `LABOR`, `EQUIPMENT`, `GENERAL`. |
| **`PARENT_KEY`** | Text | للوراثة. ضع `ENTITY_KEY` لجدول آخر لترث إعداداته. اتركها فارغة إذا لا يوجد. |
| **`LICENSE_TIER`** | Text | **(Enum)**. القيم: `Free`, `Standard`, `Premium`, `Enterprise`, `Admin`. |
| **`STORAGE_STRATEGY`**| Text | **(Enum)**. القيم: `MergeUpsert` (للبيانات المتغيرة), `ReplaceAll` (للقوائم الثابتة), `Append`. |
| **`VIEW_MODE`** | Text | **(Enum)**. القيم: `Table` (عرض شبكي), `Breakdown` (شجري), `Dashboard`. |
| **`UI_GROUP`** | Text | اسم المجموعة في القائمة العلوية (Ribbon). مثال: `Master Data`. |
| **`UI_ORDER`** | Number | رقم للترتيب (1, 2, 3...). الافتراضي في الكود 0. |
| **`ICON_KEY`** | Text | اسم الأيقونة. مثال: `Brick`, `User`, `List`. |
| **`IS_ACTIVE`** | TRUE/FALSE | هل الجدول مفعل؟ الافتراضي `TRUE`. |
| **`UX_CONFIG`** | JSON | إعدادات المظهر (انظر الأسفل). |
| **`SYS_CONFIG`** | JSON | إعدادات النظام (انظر الأسفل). |

---

### 2. حقيبة المظهر (`UX_CONFIG`)
انسخ هذا الـ JSON وضعه في عمود `UX_CONFIG`. احذف السطور التي لا تحتاجها (الكود سيعوضها بالقيم الافتراضية).

**المفاتيح (Keys) مأخوذة من كلاس `TableUxConfig`:**

```json
{
  "Color": "#FF5733",        // لون التبويب (Hex)
  "Direction": "RTL",        // اتجاه الشيت: "RTL" أو "LTR" أو "CONTEXT"
  "Freeze": "A2",            // تجميد الألواح (مثال: الصف الثاني)
  "Zoom": 100,               // نسبة التكبير (رقم)
  "Gridlines": true          // إظهار الخطوط: true أو false
}
```

---

### 3. حقيبة النظام (`SYS_CONFIG`)
انسخ هذا الـ JSON وضعه في عمود `SYS_CONFIG`.

**المفاتيح (Keys) مأخوذة من كلاس `TableSysConfig`:**

```json
{
  "PkType": "AutoGUID",      // نوع المفتاح: "AutoGUID" أو "Composite"
  "AllowEdit": false,        // هل يسمح للمستخدم بتعديل البيانات؟ true/false
  "ExportMode": true         // هل يسمح بالتصدير؟ true/false
}
```

---

### 4. أمثلة جاهزة (للتأكد من صحة الكود)

#### مثال 1: جدول خامات (أساسي)
*   **ENTITY_KEY:** `T_MATERIALS`
*   **ENTITY_TYPE:** `COST`
*   **STORAGE_STRATEGY:** `MergeUpsert`
*   **UX_CONFIG:** `{"Color": "#E67C73", "Direction": "RTL"}`
*   **SYS_CONFIG:** `{"PkType": "Composite", "AllowEdit": false}`

#### مثال 2: جدول تحليل البنود (تفاعلي)
*   **ENTITY_KEY:** `T_BOQ_BREAKDOWN`
*   **ENTITY_TYPE:** `COMP`
*   **VIEW_MODE:** `Breakdown`
*   **UX_CONFIG:** `{"Color": "#4285F4", "Freeze": "A5", "Gridlines": false}`
*   **SYS_CONFIG:** `{"AllowEdit": true, "PkType": "AutoGUID"}`

---

### ✅ نقاط القوة في الكود المرسل:
1.  **`MergeWithParent`:** الكود يدعم الوراثة بذكاء. إذا تركت `UX_CONFIG` فارغاً في الجدول الابن، سيأخذه من الأب (`ParentKey`).
2.  **`DataSanitizer`:** الكود ينظف المفاتيح (`EntityKey`) تلقائياً، لذا حتى لو كتبت مسافات بالخطأ في الإكسل، الكود سيعالجها.
3.  **`IValidatableEntity`:** الكود سيعطيك تقريراً واضحاً (`ValidationResult`) إذا نسيت عموداً إجبارياً.

هذا الملف **جاهز تماماً** ولا يحتاج لأي تعديل. يمكنك البدء بتعبئة Google Sheet بناءً على هذا الدليل.


{
  "Color": "#FF5733",
  "Direction": "RTL",
  "Freeze": "A2",
  "Zoom": 90,
  "Gridlines": false
}
