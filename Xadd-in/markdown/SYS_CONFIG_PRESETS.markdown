إليك "دليل المستخدم الاحترافي" (Professional User Guide) للجداول الجديدة التي ستحول منظومتك إلى نظام **Metaprogramming** كامل، حيث يتم التحكم في الواجهة والمنطق من خلال البيانات فقط.

---

# 1️⃣ الجدول الأول: SYS_CONFIG_PRESETS
**الوظيفة:** "مستودع القوالب". يُستخدم لإلغاء كتابة الـ JSON المتكرر وتوحيد الإعدادات عبر النظام.

### 📋 أعمدة الجدول (Headers)

| # | اسم العمود (Header) | النوع | الوصف والدور | أمثلة |
| :--- | :--- | :--- | :--- | :--- |
| **1** | **`PRESET_KEY`** | `String` | **مفتاح القالب.** المعرف الذي ستستخدمه في الجداول الأخرى. | `UX_DARK_TABLE`, `LOGIC_STANDARD_PRICING` |
| **2** | **`PROP_KEY`** | `String` | **اسم الخاصية.** نفس المفاتيح التي كانت داخل الـ JSON. | `Color`, `Direction`, `Precision`, `ShowTotal` |
| **3** | **`PROP_VALUE`** | `String` | **القيمة.** القيمة المراد تطبيقها. | `#1A1A1A`, `RTL`, `2`, `TRUE` |
| **4** | **`VAL_TYPE`** | `Enum` | **نوع القيمة.** يساعد الكود في عملية الـ Parsing. | `String`, `Number`, `Boolean`, `Color` |
| **5** | **`CATEGORY`** | `String` | **التصنيف.** (اختياري) لفرز الإعدادات. | `UI`, `LOGIC`, `SECURITY` |
| **6** | **`DESCRIPTION`** | `String` | **شرح.** لتذكير المطور بوظيفة هذا السطر. | `يجعل خلفية الجدول داكنة` |

### 💡 مثال للتطبيق (كيف يقرأ الكود؟)
إذا كتبت في جدول `SYS_DEFINITIONS` أن القالب هو `UX_DARK_TABLE` سيقوم المحرك بجمع كل الأسطر التي تحمل هذا الاسم وتحويلها تلقائياً إلى:
```json
{ "Color": "#1A1A1A", "Direction": "RTL", "ShowTotal": true }
```

---

# 2️⃣ الجدول الثاني: SYS_UI_NAVIGATION
**الوظيفة:** "مهندس الواجهات". هو المسؤول عن رسم القوائم، الأزرار، والروابط بين الجداول (Menu List & Ribbon).

### 📋 أعمدة الجدول (Headers)

| # | اسم العمود (Header) | النوع | الوصف والدور | أمثلة |
| :--- | :--- | :--- | :--- | :--- |
| **1** | **`NAV_KEY`** | `String` | **معرف العنصر.** اسم فريد للزر أو القائمة. | `BTN_IMPORT_ESTIMATE`, `MENU_MASTER_DATA` |
| **2** | **`PARENT_KEY`** | `String` | **الأب.** إذا كان الزر داخل قائمة منسدلة، ضع مفتاح القائمة الأب هنا. | `MENU_MASTER_DATA`, `null` |
| **3** | **`DISPLAY_NAME`** | `String` | **الاسم الظاهر.** النص الذي يراه المستخدم على الزر (يدعم العربية). | `تحديث قاعدة البيانات` |
| **4** | **`ICON_KEY`** | `String` | **الأيقونة.** اسم الأيقونة البرمجي. | `CloudUpload`, `Settings`, `Play` |
| **5** | **`ACTION_TYPE`** | `Enum` | **نوع الإجراء.** ماذا يحدث عند الضغط؟ | `OPEN_TABLE`, `RUN_PROCEDURE`, `GENERATE_REPORT` |
| **6** | **`TARGET_ENTITY`** | `String` | **الهدف.** اسم الجدول (`ENTITY_KEY`) أو اسم الوظيفة البرمجية. | `T_MATERIALS`, `COST_ENGINE_V1` |
| **7** | **`UI_ORDER`** | `Int` | **الترتيب.** ترتيب ظهور الزر من اليسار لليمين. | `10, 20, 30...` |
| **8** | **`VISIBILITY_LEVEL`**| `String` | **الصلاحيات.** من يمكنه رؤية هذا الزر؟ | `Admin`, `Standard`, `Free` |
| **9** | **`ACTION_CONFIG`** | `JSON/ID` | **إعدادات إضافية.** بارامترات ترسل للوظيفة عند الضغط. | `{"Filter": "Active"}` |

---

### 🚀 سيناريو احترافي: كيف يعمل المحرك الآن؟

تخيل أنك تريد إضافة **"زر لحساب تكلفة المشروع"** في القائمة العلوية:

1.  **في جدول `SYS_UI_NAVIGATION`:**
    *   `NAV_KEY`: `BTN_CALC_PROJECT`
    *   `DISPLAY_NAME`: `حساب التكاليف النهائية`
    *   `ACTION_TYPE`: `RUN_PROCEDURE`
    *   `TARGET_ENTITY`: `PRICE_ENGINE`
    *   `ICON_KEY`: `CalculatorIcon`

2.  **عند ضغط المستخدم:**
    *   الكود يذهب لجدول `SYS_DATA_MODEL` ليفهم العلاقات.
    *   يذهب لجدول `SYS_SCHEMA_RULES` ليعرف أي الأعمدة تمثل السعر (`PRICE`) وأيها الكمية (`QTY`).
    *   يذهب لجدول `SYS_CONFIG_PRESETS` ليجلب عدد الأرقام العشرية المطلوبة (`Precision`).
    *   ينفذ العملية ويظهر النتيجة.

---

### 🛠 نصائح إضافية لإدارة الأخطاء (Data Integrity):

1.  **قاعدة الـ 10 أضعاف في الترتيب:** في عمود `UI_ORDER` استخدم دائماً (10، 20، 30) بدلاً من (1، 2، 3). هذا يسمح لك مستقبلاً بإضافة زر بين زرين بسهولة (مثلاً تضع رقمه 15) دون إعادة ترقيم كل الجدول.
2.  **الأسماء الثابتة (Naming Convention):**
    *   ابدأ مفاتيح الـ UX بـ `UX_`.
    *   ابدأ مفاتيح المنطق بـ `LOGIC_`.
    *   ابدأ مفاتيح الأزرار بـ `BTN_`.
    هذا يسهل عليك البحث والفلترة داخل Google Sheets عندما يصبح لديك مئات الأسطر.

**بهذين الجدولين، أنت لم تعد تبني "كوداً لجدول"، بل تبني "مصنعاً للتطبيقات" (Application Factory). هل نبدأ في مناقشة كود المحرك الذي سيقوم بدمج كل هذه الجداول معاً؟**
