
# 📘  : SYS_SCHEMA_RULES

**الوظيفة:** تحديد هيكل الأعمدة، أنواع البيانات، قواعد العرض، ومنطق محرك التسعير.

### أولاً: الأعمدة الصريحة (Explicit Columns)
هذه الأعمدة إجبارية، وتؤثر مباشرة على قاعدة البيانات وكفاءة النظام.

| # | اسم العمود (Header) | النوع | الوصف والدور | القيم المسموحة / أمثلة |
| :--- | :--- | :--- | :--- | :--- |
| **1** | **`ENTITY_KEY`** | `String` | **اسم الجدول (FK).**<br>ربط العمود بجدوله في `SYS_DEFINITIONS`. | `T_MATERIALS` |
| **2** | **`ATTRIBUTE_KEY`** | `String` | **اسم العمود البرمجي.**<br>اسم العمود في SQL والكود. (إنجليزي، فريد). | `UNIT_PRICE` |
| **3** | **`DISPLAY_HEADER`** | `String` | **عنوان العمود.**<br>الاسم الظاهر للمستخدم في الإكسل (يدعم العربية). | `سعر الوحدة` |
| **4** | **`LICENSE_TIER`** | `String` | **الباقة | `Free` |
| **5** | **`ORDINAL_POS`** | `Int` | **الترتيب.**<br>ترتيب ظهور العمود (1, 2, 3...). | `10`, `20` |
| **6** | **`SEMANTIC_ROLE`** | `String` | **دور العمود (مخ النظام).**<br>يخبر المحرك بوظيفة العمود للحسابات التلقائية. | • `PRICE` (للسعر والعملة)<br>• `QTY` (للكمية)<br>• `TOTAL` (للإجمالي)<br>• `UNIT` (وحدة القياس)<br>• `NAME` (اسم البند)<br>• `NONE` (بدون دور) |
| **7** | **`DATA_TYPE`** | `String` | **نوع البيانات (SQL).**<br>نوع التخزين في قاعدة البيانات. | • `DECIMAL` (أرقام عشرية)<br>• `INT` (أرقام صحيحة)<br>• `TEXT` (نصوص)<br>• `BOOL` (نعم/لا)<br>• `DATE` (تاريخ) |
| **8** | **`IS_PK`** | `Bool` | **مفتاح أساسي.**<br>هل يميز السطر؟ (هام للتحديث). | `TRUE`, `FALSE` |
| **9** | **`IS_MANDATORY`** | `Bool` | **إجباري (Not Null).**<br>هل يمنع حفظ السطر إذا كان فارغاً؟ | `TRUE`, `FALSE` |
| **10** | **`IS_VIRTUAL`** | `Bool` | **وهمي/محسوب.**<br>هل هو عمود معادلة فقط (لا يخزن في الداتابيز)؟ | `TRUE` (معادلة فقط)<br>`FALSE` (حقيقي) |
| **11** | **`IS_VISIBLE`** | `Bool` | **الظهور.**<br>هل يظهر للمستخدم في الإكسل؟ | `TRUE` (ظاهر)<br>`FALSE` (مخفي) |

---

### ثانياً: أعمدة الإعدادات (JSON Bags)

#### العمود 12: `UX_CONFIG` (تجميل الإكسل)
أي خاصية تخص الشكل فقط.

| المفتاح (Key) | الوصف | القيم / أمثلة |
| :--- | :--- | :--- |
| **`"Width"`** | عرض العمود (pixels). | `100`, `150` |
| **`"Align"`** | المحاذاة. | `"Center"`, `"Right"`, `"Left"` |
| **`"Format"`** | تنسيق الأرقام/التواريخ. | `"#,##0.00"`, `"dd/mm/yyyy"` |
| **`"Color"`** | لون خلفية العمود (Hex). | `"#F9E79F"` |
| **`"Wrap"`** | التفاف النص الطويل. | `true`, `false` |
| **`"Tooltip"`** | رسالة تلميح عند الوقوف على العنوان. | `"السعر يشمل الضريبة"` |

#### العمود 13: `LOGIC_CONFIG` (السلوك المتقدم)
أي خاصية تخص المنطق والقواعد.

| المفتاح (Key) | الوصف | القيم / أمثلة |
| :--- | :--- | :--- |
| **`"Formula"`** | معادلة الإكسل (للأعمدة الـ Virtual). | `=[@Price]*[@Qty]` |
| **`"Default"`** | القيمة الافتراضية. | `0`, `"عام"` |
| **`"Summary"`** | طريقة التجميع في الـ Breakdown. | `"SUM"`, `"AVG"`, `"COUNT"` |
| **`"Validation"`** | قائمة منسدلة (Dropdown). | `"Ton,Kg,m3"` |
| **`"License"`** | حجب العمود (Security Tier). | `"Premium"`, `"Admin"` |

---

### ثالثاً: أمثلة عملية للتعبئة (سيناريوهات حقيقية)

استخدم هذه الصفوف كنواة لبناء الجدول الخاص بك.

#### 1. السيناريو: عمود "السعر" (عمود رقمي، إجباري، مخصص للمحرك)
*   **ENTITY_KEY:** `T_MATERIALS`
*   **ATTRIBUTE_KEY:** `UNIT_PRICE`
*   **DISPLAY_HEADER:** `سعر الوحدة`
*   **SEMANTIC_ROLE:** `PRICE`
*   **DATA_TYPE:** `DECIMAL`
*   **IS_PK:** `FALSE`
*   **IS_MANDATORY:** `TRUE`
*   **IS_VIRTUAL:** `FALSE`
*   **IS_VISIBLE:** `TRUE`
*   **UX_CONFIG:** `{"Width": 120, "Format": "#,##0.00", "Color": "#EAFAF1"}`
*   **LOGIC_CONFIG:** `{"Default": 0, "Summary": "AVG"}`

#### 2. السيناريو: عمود "الإجمالي" (عمود معادلة، لا يخزن)
*   **ENTITY_KEY:** `T_MATERIALS`
*   **ATTRIBUTE_KEY:** `TOTAL_COST`
*   **DISPLAY_HEADER:** `الإجمالي`
*   **SEMANTIC_ROLE:** `TOTAL`
*   **DATA_TYPE:** *(اتركه فارغاً)*
*   **IS_PK:** `FALSE`
*   **IS_MANDATORY:** `FALSE`
*   **IS_VIRTUAL:** `TRUE`
*   **IS_VISIBLE:** `TRUE`
*   **UX_CONFIG:** `{"Width": 130, "Format": "#,##0.00 $", "Color": "#D6EAF8"}`
*   **LOGIC_CONFIG:** `{"Formula": "=[@UNIT_PRICE]*[@QTY]", "Summary": "SUM"}`

#### 3. السيناريو: عمود "وحدة القياس" (قائمة منسدلة)
*   **ENTITY_KEY:** `T_MATERIALS`
*   **ATTRIBUTE_KEY:** `UOM`
*   **DISPLAY_HEADER:** `الوحدة`
*   **SEMANTIC_ROLE:** `UNIT`
*   **DATA_TYPE:** `TEXT`
*   **IS_PK:** `FALSE`
*   **IS_MANDATORY:** `TRUE`
*   **IS_VIRTUAL:** `FALSE`
*   **IS_VISIBLE:** `TRUE`
*   **UX_CONFIG:** `{"Width": 80, "Align": "Center"}`
*   **LOGIC_CONFIG:** `{"Validation": "Ton,Kg,m3,Ltr,Pcs", "Default": "Ton"}`

#### 4. السيناريو: عمود "المعرف الداخلي" (مخفي، مفتاح أساسي)
*   **ENTITY_KEY:** `T_MATERIALS`
*   **ATTRIBUTE_KEY:** `MAT_ID`
*   **DISPLAY_HEADER:** `ID`
*   **SEMANTIC_ROLE:** `NONE`
*   **DATA_TYPE:** `TEXT`
*   **IS_PK:** `TRUE`
*   **IS_MANDATORY:** `TRUE`
*   **IS_VIRTUAL:** `FALSE`
*   **IS_VISIBLE:** `FALSE`
*   **UX_CONFIG:** `{}`
*   **LOGIC_CONFIG:** `{}`

---

هذا الجدول هو "دستور البيانات" لديك. أي تغيير في شكل أو سلوك البرنامج يبدأ من هنا. ابدأ بإنشائه الآن.
