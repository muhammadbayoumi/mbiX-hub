هذا الجدول هو **"لوحة التحكم" (Control Panel)** للمشروع.
بدلاً من أن تسأل المستخدم في كل مرة عن "نسبة الضريبة" أو "سعر الوقود"، تخزنها هنا وتجعل كل المعادلات تقرأ منها.

إليك التصميم التفصيلي لجدول **`T_PROJECT_PARAMS`** (ثوابت ومتغيرات المشروع) كما يجب تعريفه في `SYS_SCHEMA_RULES`.

---

# 🏗️ هيكل جدول: T_PROJECT_PARAMS

يجب تعريف هذا الجدول أولاً في `SYS_DEFINITIONS` بنوع `REF` (مرجعي).

### 1. الأعمدة الأساسية (Structure & Identity)

| ATTRIBUTE_KEY | DISPLAY_HEADER | SEMANTIC_ROLE | DATA_TYPE | IS_PK | وصف الدور |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **`PARAM_KEY`** | **كود المتغير** | `ID` | `TEXT` | `TRUE` | المعرف البرمجي (يستخدم في المعادلات). مثال: `VAT_RATE`. |
| **`PARAM_VALUE`** | **القيمة** | `VALUE` | `TEXT` | `FALSE` | القيمة الفعلية. نخزنها كنص، والكود يحولها حسب الـ DataType. |
| **`DISPLAY_NAME`** | **البيان** | `NAME` | `TEXT` | `FALSE` | الاسم العربي الظاهر للمستخدم. مثال: "ضريبة القيمة المضافة". |
| **`DATA_TYPE`** | **نوع البيانات** | `TYPE` | `TEXT` | `FALSE` | كيف نعالج القيمة؟ (`PERCENT`, `MONEY`, `DATE`, `TEXT`). |
| **`GROUP_NAME`** | **التصنيف** | `GROUP` | `TEXT` | `FALSE` | لترتيب الإعدادات في شاشة المستخدم (مالي، لوجستي، عام). |

---

### 2. أعمدة التحكم والعرض (UX & Logic)

| ATTRIBUTE_KEY | UX_CONFIG (JSON) | LOGIC_CONFIG (JSON) | الشرح |
| :--- | :--- | :--- | :--- |
| **`DESCRIPTION`** | `{"Width": 250, "Wrap": true}` | `{}` | شرح إضافي للمستخدم حول كيفية تأثير هذا المتغير. |
| **`IS_LOCKED`** | `{"Visible": false}` | `{"Default": false}` | إذا كان `TRUE`، لا يستطيع المستخدم تعديله (ثابت نظام). |
| **`VALIDATION_RULE`** | `{}` | `{"Validation": "0-100"}` | (اختياري) لتقييد الإدخال (مثلاً نسبة لا تزيد عن 100). |

---

### 3. أمثلة عملية لمحتوى هذا الجدول (Data Examples)

إليك كيف ستبدو البيانات فعلياً داخل هذا الجدول، وكيف تستخدمها.

#### المجموعة أ: ثوابت مالية (Financial)

| PARAM_KEY | PARAM_VALUE | DATA_TYPE | DISPLAY_NAME | GROUP_NAME |
| :--- | :--- | :--- | :--- | :--- |
| **`VAT_RATE`** | `0.14` | `PERCENT` | ضريبة القيمة المضافة | Financial |
| **`PROFIT_MARGIN`** | `0.20` | `PERCENT` | هامش الربح المستهدف | Financial |
| **`RISK_FACTOR`** | `0.05` | `PERCENT` | نسبة المخاطر | Financial |
| **`CURRENCY_RATE`** | `50.5` | `DECIMAL` | سعر صرف الدولار | Financial |

#### المجموعة ب: ثوابت لوجستية (Logistics)

| PARAM_KEY | PARAM_VALUE | DATA_TYPE | DISPLAY_NAME | GROUP_NAME |
| :--- | :--- | :--- | :--- | :--- |
| **`FUEL_PRICE`** | `13.50` | `MONEY` | سعر لتر السولار | Logistics |
| **`SITE_DISTANCE`** | `150` | `INT` | المسافة للموقع (كم) | Logistics |
| **`ROAD_FACTOR`** | `1.2` | `DECIMAL` | معامل وعورة الطريق | Logistics |

#### المجموعة ج: معلومات المشروع (General)

| PARAM_KEY | PARAM_VALUE | DATA_TYPE | DISPLAY_NAME | GROUP_NAME |
| :--- | :--- | :--- | :--- | :--- |
| **`START_DATE`** | `2024-01-01` | `DATE` | تاريخ بدء المشروع | General |
| **`DURATION`** | `12` | `INT` | مدة المشروع (شهر) | General |

---

### 4. كيف يستخدم النظام هذا الجدول؟ (The Integration)

الآن، لنربط هذا الجدول بجدول الـ Breakdown.

**السيناريو:**
نريد حساب سعر الخرسانة، ولكن نريد إضافة "هامش الربح" و "نسبة المخاطر" تلقائياً.

1.  **في جدول المعادلات (`SYS_TRANSFORM_MAPS` أو `LOGIC_CONFIG` للعمود):**
    تكتب المعادلة بهذا الشكل (Pseudo-code):

    ```excel
    Total_Price = [Direct_Cost] * (1 + GET_PARAM('PROFIT_MARGIN') + GET_PARAM('RISK_FACTOR'))
    ```

2.  **تطبيق تكلفة النقل:**
    إذا كان لديك بند نقل، يمكنك حساب تكلفته بناءً على المسافة وسعر الوقود:

    ```excel
    Transport_Cost = [Qty] * GET_PARAM('SITE_DISTANCE') * GET_PARAM('FUEL_PRICE') * 0.05
    ```

### الخلاصة
جدول `T_PROJECT_PARAMS` هو **الذاكرة المركزية** للمشروع.
*   يجعل تعديل الأسعار العالمية (مثل الوقود أو الدولار) أمراً سهلاً جداً: عدل الرقم هنا، يتغير تسعير المقايسة بالكامل (1000 بند) في لحظة واحدة.
