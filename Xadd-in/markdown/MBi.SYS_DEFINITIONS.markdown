# 📘 الجدول المرجعي : SYS_DEFINITIONS

عدد الأعمدة الكلي: **14 عموداً**.
(12 عمود صريح + 2 عمود JSON متخصص).

### 1. الأعمدة الصريحة (Core Columns)
هذه الأعمدة إجبارية ومفصولة لسهولة القراءة والفلترة.

| # | اسم العمود (Header) | النوع | الوصف | أمثلة |
| :--- | :--- | :--- | :--- | :--- |
| **1** | **`ENTITY_KEY`** | `String` | **المعرف الفريد (PK).** | `T_MATERIALS` |
| **2** | **`DISPLAY_NAME`** | `String` | **اسم العرض.** | `أسعار الخامات` |
| **3** | **`ENTITY_TYPE`** | `String` | **نوع المحرك.** (`COST`, `PERF`, `REF`, `COMP`) | `COST` |
| **4** | **`BUSINESS_DOMAIN`** | `String` | **المجال الوظيفي.** | `MATERIAL`, `LABOR` |
| **5** | **`PARENT_KEY`** | `String` | **الوراثة (Parent Template).**<br>اسم الجدول الذي نرث منه الأعمدة. | `T_TEMPLATE_COST` |
| **6** | **`LICENSE_TIER`** | `String` | **الباقة.** | `Free`, `Premium` |
| **7** | **`STORAGE_STRATEGY`**| `String` | **طريقة التحديث.** | `MergeUpsert` |
| **8** | **`VIEW_MODE`** | `String` | **طريقة العرض.** | `Table`, `Breakdown` |
| **9** | **`UI_GROUP`** | `String` | **مجموعة القائمة.** | `Resources` |
| **10** | **`UI_ORDER`** | `Int` | **الترتيب.** | `10` |
| **11** | **`ICON_KEY`** | `String` | **الأيقونة.** | `Brick` |
| **12** | **`IS_ACTIVE`** | `Bool` | **التفعيل.** | `TRUE` |

---

### 2. أعمدة الـ JSON المقسمة (Specialized Bags)

بدلاً من حقيبة واحدة كبيرة، قمنا بتقسيمها إلى اثنتين:

#### العمود 13: `UX_CONFIG` (حقيبة الشكل والمظهر)
كل ما يخص الألوان وتنسيق الإكسل فقط.

| المفتاح | الوصف | مثال |
| :--- | :--- | :--- |
| `"Color"` | لون التبويب. | `"#FF0000"` |
| `"Direction"` | اتجاه الشيت. | `"RTL"` |
| `"Freeze"` | تجميد الصفوف. | `"A2"` |
| `"Zoom"` | نسبة التكبير. | `100` |
| `"Gridlines"` | إظهار الشبكة. | `true` |

#### العمود 14: `SYS_CONFIG` (حقيبة السلوك المتقدم)
الإعدادات التقنية الإضافية التي لا تستخدم كثيراً ولكنها مهمة.

| المفتاح | الوصف | مثال |
| :--- | :--- | :--- |
| `"PkType"` | نوع توليد المفاتيح. | `"AutoGUID"`, `"Composite"` |
| `"AllowEdit"` | السماح للمستخدم بالتعديل اليدوي. | `true` |
| `"ExportMode"` | هل الجدول قابل للتصدير PDF؟ | `true` |

---

### 3. أمثلة عملية للتعبئة (جاهزة للنسخ)

#### الصف 1: جدول أسعار الخامات
*   **ENTITY_KEY:** `T_MATERIALS`
*   **PARENT_KEY:** `NULL`
*   **UX_CONFIG:** `{ "Color": "#FF8C00", "Direction": "RTL", "Freeze": "A2" }`
*   **SYS_CONFIG:** `{ "PkType": "Composite", "AllowEdit": false }`

#### الصف 2: جدول إنتاجية العمالة (يرث من قالب)
*   **ENTITY_KEY:** `T_LABOR_RATES`
*   **PARENT_KEY:** `T_TEMPLATE_PERF` *(لأخذ نفس أعمدة الإنتاجية)*
*   **UX_CONFIG:** `{ "Color": "#28B463", "Direction": "RTL" }`
*   **SYS_CONFIG:** `{ "PkType": "AutoGUID" }`

#### الصف 3: تحليل البنود
*   **ENTITY_KEY:** `T_BOQ_BREAKDOWN`
*   **ENTITY_TYPE:** `COMP`
*   **UX_CONFIG:** `{ "Color": "#2E86C1", "Direction": "RTL", "Freeze": "A5" }`
*   **SYS_CONFIG:** `{ "AllowEdit": true, "PkType": "Composite" }`

### لماذا هذا التقسيم هو الأفضل؟
1.  **وضوح المسؤوليات:** المصمم يتعامل مع عمود `UX`، والمطور يتعامل مع عمود `SYS` والأعمدة الصريحة.
2.  **الوراثة:** وجود `PARENT_KEY` كعمود صريح يسهل على الكود بناء شجرة الوراثة بسرعة.
3.  **السهولة:** الـ JSON أصبح أصغر وأسهل في القراءة والتعديل داخل Google Sheets.

يمكنك الآن إنشاء الجدول بهذا الهيكل (14 عمود).
